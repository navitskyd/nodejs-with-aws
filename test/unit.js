// test that the routes file exports both routes

// test that the config file exports .env variables

// test that the config file throws error when it doesn't get the env variables

// test that the search sets default values when no parameters are received

// test that the search sets correct ES params when parameters are received

// test the search results are coherent when search params are sent

// LOW test the bucket putObject returns promise (?)

// test for rollback if S3 upload fails

// test for commit if all works

// test DB config sets up correctly